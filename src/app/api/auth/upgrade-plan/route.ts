import { NextResponse } from "next/server";

/** Placeholder: wire subscription updates when billing is connected to Supabase. */
export async function POST() {
  return NextResponse.json({ ok: true });
}
