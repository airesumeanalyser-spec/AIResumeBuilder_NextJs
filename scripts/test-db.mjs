#!/usr/bin/env node
/**
 * DB smoke test — apply supabase/migrations/*.sql in the Supabase SQL editor.
 */
console.log(
  "Apply supabase/migrations in your Supabase project, then use the app to verify."
);
process.exit(0);
