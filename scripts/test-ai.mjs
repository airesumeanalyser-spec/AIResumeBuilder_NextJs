#!/usr/bin/env node
/**
 * Vertex AI smoke test — run the app locally with GCP_PROJECT_ID set,
 * or use: npx tsx scripts/test-ai-runner.mts (add if needed).
 */
console.log("Configure GCP_PROJECT_ID and use the /upload flow to test AI.");
process.exit(0);
